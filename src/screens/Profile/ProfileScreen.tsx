import React from 'react'

import UnderConstruction from '@components/UnderConstruction'

const ProfileScreen: React.FC = () => {
  return <UnderConstruction name={'Profile Screen'} />
}

export default ProfileScreen
